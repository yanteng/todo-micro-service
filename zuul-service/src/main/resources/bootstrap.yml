spring:
  application:
    name: zuul-service
  profiles:
    active: container
  cloud:
    config:
      uri: http://localhost:8081

#spring:
#  config:
#    location: ../config/zuul-service/application.yml
