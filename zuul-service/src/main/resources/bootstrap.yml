spring:
  application:
    name: zuul-service
  profiles:
    active: container
  cloud:
    config:
      uri: http://config-service:8888

#spring:
#  config:
#    location: ../config/zuul-service/application.yml
