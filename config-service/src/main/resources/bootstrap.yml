spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yanteng/todo-micro-service.git
          search-paths: cloud-config
          timeout: 60
server:
  port: 8081