spring:
  application:
    name: user-service
  profiles:
    active: container
  cloud:
    config:
      uri: http://localhost:8888

#spring:
#  config:
#    location: ../config/todo-service/application.yml