spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8081

#spring:
#  config:
#    location: ../config/todo-service/application.yml