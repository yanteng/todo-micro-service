spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://config-service:8888

#spring:
#  config:
#    location: ../config/todo-service/application.yml